node_modules
dist
.env

# Stitch generated assets (fetched via MCP / curl -L)
# public/stitch/ hero images (4 PNGs, ~230KB) are now tracked for Vercel — required for "Real people. Real research" section
# (previously ignored, causing 404 on Vercel; remote fallback added in src/main.jsx as well)
stitch_html/
**/stitch_screens/
dist/stitch/

# Editor - VS Code
.vscode/

# Microsoft Word documents
*.docx
~$*.docx
